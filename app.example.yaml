runtime: nodejs
env: flex
service: nestjs-server

# instance_class: F1
# automatic_scaling:
#   max_instances: 20

env_variables:
  NODE_PORT: 8080
  DATABASE_URL: $DATABASE_URL

beta_settings:
 cloud_sql_instances: ecgproject-1069:us-central1:ae86
